
<script>

     var data = @Html.Raw(Json.Encode(ViewBag.DATA));
    $(document).ready(function () {
        for (var i = 0; i < (@Model.TileZones.Count); i++) {
            customChart("chart" + i, data);
        }

        @*@foreach (var t in Model.TileZones)
        {
            @: $.get('@Url.Action("GetJson","Home",new {data = t.DashItem.Graphdata })', customChart("chart1",result));*@
            
            @*@: customChart("chart1",@t.DashItem.Graphdata);*@
        //}

        $(window).resize(function () {
            window.customChart.redraw();
        });
    });

    function customChart(id, data) {
        Morris.Area({
            // ID of the element in which to draw the chart.
            element: id,
            // Chart data records -- each entry in this array corresponds to a point on
            // the chart.
            data: data,
            // The name of the data record attribute that contains x-values.
            xkey: 'label',
            ykeys: ['value'],
            labels: ['aantal tweets Van Verhofstad'],
            pointSize: 2,
            hideHover: 'auto',
            resize: true,
            redraw: true
        });
    }
</script>